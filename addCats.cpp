///////////////////////////////////////////////////////////////////////////////
///         University of Hawaii, College of Engineering
/// @brief  ee205-Animal-Farm - EE 205 - Spr 2022
///
/// @file addCats.c
/// @version 1.0
///
/// @author Caleb Mueller <mc61@hawaii.edu>
/// @date   15_Mar_2022
///////////////////////////////////////////////////////////////////////////////
#include <cassert>
#include <iostream>

#include "addCats.h"
#include "catDatabase.h"
#include "config.h"

/// @param newCat dynamically allocated cat will be added.  Must not be nullptr
/// @return True if successful, else False
/// @throws logic_error if newCat is already in database

bool addCat(Cat* newCat)
{
    /*
    assert(newCat != nullptr);
    newCat ->validate();

    if(catIsInDatabase(newCat))
    {
        throw logic_error( PROGRAM_NAME ": Cat is already in database!");
    }
     */
}